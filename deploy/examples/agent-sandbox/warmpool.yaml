apiVersion: extensions.agents.x-k8s.io/v1beta1
kind: SandboxWarmPool
metadata:
  name: playwright
  namespace: NAMESPACE
spec:
  replicas: 2
  sandboxTemplateRef:
    name: playwright
  updateStrategy:
    type: OnReplenish
